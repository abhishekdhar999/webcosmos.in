/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    basePath: '/webcosmos.in', 
    assetPrefix: '/webcosmos.in/',
    output: 'export',
};

export default nextConfig;
